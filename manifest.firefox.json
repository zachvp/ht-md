{
  "_comment": "Overrides merged onto manifest.base.json for the Firefox build. Firefox's MV3 doesn't support background.service_worker yet, so it uses the scripts-array event-page form.",
  "browser_specific_settings": {
    "gecko": {
      "id": "web-md@local",
      "data_collection_permissions": {
        "required": ["none"]
      }
    }
  },
  "background": {
    "scripts": ["background.js"]
  }
}
