{
  "_comment": "Overrides merged onto manifest.base.json for Chromium-based builds (Chrome, Brave, Edge). MV3 requires background.service_worker here.",
  "background": {
    "service_worker": "background.js"
  }
}
