{
  "name": "web-md",
  "version": "1.0.0",
  "description": "",
  "main": "background.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "eslint 'src/*.ts'",
    "build:js": "mkdir -p dist && esbuild 'src/*.ts' --bundle --platform=browser --outdir=dist && cp content.css turndown.js dist/ && cp -r icons dist/",
    "build:firefox": "npm run build:js && cp manifest.json dist/manifest.json && cd dist && zip -r ../web-md.xpi . && cd ..",
    "build:chrome": "npm run build:js && cp manifest.chrome.json dist/manifest.json && cd dist && zip -r ../web-md-chrome.zip . && cd ..",
    "build": "npm run build:firefox",
    "typecheck": "tsc --noEmit",
    "gen-icons": "python3 scripts/gen-icons.py"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@types/chrome": "^0.1.42",
    "@typescript-eslint/eslint-plugin": "^8.59.4",
    "@typescript-eslint/parser": "^8.59.4",
    "esbuild": "^0.28.0",
    "eslint": "^10.4.0",
    "globals": "^17.6.0",
    "typescript": "^6.0.3"
  }
}
